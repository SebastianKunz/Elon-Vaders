{
  "name": "p5-typescript-starter",
  "version": "1.0.0",
  "description": "SpaceInvader submission for IT-Talents competition",
  "scripts": {
    "start": "run-p start-compile start-run",
    "start-compile": "tsc --watch",
    "start-run": "browser-sync start --server -w"
  },
  "devDependencies": {
    "@types/p5": "^0.9.0",
    "npm-run-all": "^4.1.5",
    "browser-sync": "^2.27.10",
    "typescript": "^3.6.3"
  }
}
